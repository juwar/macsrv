[metadata]
name = macsrv
version = 1.0.0
description = Prevent macOS sleep until a target time

[options]
package_dir =
    = src
packages = find:
python_requires = >=3.9

[options.packages.find]
where = src

[options.entry_points]
console_scripts =
    macsrv = macsrv.cli:main