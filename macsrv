#!/usr/bin/env python3
"""macsrv launcher — wraps the package for direct execution."""
import sys
import os
from pathlib import Path

# Add src to path so the package is importable
SRC = Path(__file__).resolve().parent / "src"
sys.path.insert(0, str(SRC))

from macsrv.cli import main
sys.exit(main())